import setNotebookSelectedStatus from 'ape-note/utils/set-notebook-selected-status';
import { module, test } from 'qunit';

module('Unit | Utility | set notebook selected status');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = setNotebookSelectedStatus();
  assert.ok(result);
});
