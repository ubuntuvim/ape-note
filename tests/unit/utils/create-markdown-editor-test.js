import createMarkdownEditor from 'ape-note/utils/create-markdown-editor';
import { module, test } from 'qunit';

module('Unit | Utility | create markdown editor');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = createMarkdownEditor();
  assert.ok(result);
});
