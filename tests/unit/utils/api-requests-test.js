import apiRequests from 'ape-note/utils/api-requests';
import { module, test } from 'qunit';

module('Unit | Utility | api requests');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = apiRequests();
  assert.ok(result);
});
